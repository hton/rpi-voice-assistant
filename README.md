# Голосовой Ассистент для Raspberry Pi

Полнофункциональный голосовой ассистент на русском языке для Raspberry Pi с поддержкой локальной LLM, управления умным домом, напоминаний и интеграции с Google Calendar.

## Возможности

- 🎤 **Распознавание речи** - офлайн распознавание русской речи через Vosk
- 🔊 **Синтез речи** - качественный голосовой вывод через Piper TTS
- 🤖 **LLM интеграция** - локальные языковые модели через HuggingFace Transformers
- 🏠 **Умный дом** - управление устройствами через GPIO и MQTT
- ⏰ **Напоминания** - система напоминаний и интеграция с Google Calendar
- 🌤️ **Информация** - погода и новости
- 🔘 **Активация** - кнопка GPIO или ключевое слово
- 🔄 **Автозапуск** - systemd service для автоматического запуска

## Требования

### Аппаратное обеспечение
- Raspberry Pi 4 (4-8GB RAM рекомендуется)
- USB микрофон или USB звуковая карта с микрофоном
- Динамики или наушники
- (Опционально) Кнопка для GPIO активации
- (Опционально) LED индикатор

### Программное обеспечение
- Raspberry Pi OS (64-bit рекомендуется)
- Python 3.8+
- Минимум 4GB свободного места на SD карте

## Установка

### 1. Подготовка системы

```bash
# Обновление системы
sudo apt-get update && sudo apt-get upgrade -y

# Установка зависимостей
sudo apt-get install -y \
    python3-pip python3-venv \
    portaudio19-dev python3-pyaudio \
    espeak espeak-ng \
    git wget curl unzip \
    libasound2-dev \
    libportaudio2 libportaudiocpp0

# Для MQTT (опционально)
sudo apt-get install -y mosquitto mosquitto-clients
```

### 2. Клонирование проекта

```bash
cd ~
git clone <repository-url> rpi-voice-assistant
cd rpi-voice-assistant
```

### 3. Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate
```

### 4. Установка Python зависимостей

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

**Примечание**: Установка PyTorch и Transformers может занять 20-30 минут на Raspberry Pi.

### 5. Загрузка моделей

#### Vosk (Speech Recognition)

```bash
cd models
wget https://alphacephei.com/vosk/models/vosk-model-small-ru-0.22.zip
unzip vosk-model-small-ru-0.22.zip
rm vosk-model-small-ru-0.22.zip
cd ..
```

#### Piper TTS (Text-to-Speech)

```bash
# Установка Piper
pip install piper-tts

# Скачивание русской модели
mkdir -p models/piper
cd models/piper

# Загрузка модели Ruslan (качественный русский голос)
wget https://huggingface.co/rhasspy/piper-voices/resolve/main/ru/ru_RU/ruslan/medium/ru_RU-ruslan-medium.onnx
wget https://huggingface.co/rhasspy/piper-voices/resolve/main/ru/ru_RU/ruslan/medium/ru_RU-ruslan-medium.onnx.json

cd ../..
```

#### LLM модель (HuggingFace)

Модель будет загружена автоматически при первом запуске. Вы можете изменить модель в `config/config.yaml`.

Рекомендуемые модели для RPi 4:
- `ai-forever/rugpt3large_based_on_gpt2` (легкая, ~1.5GB)
- `IlyaGusev/saiga_mistral_7b` (продвинутая, требует 8GB RAM + 4-bit квантизация)

### 6. Настройка конфигурации

```bash
# Копирование примера .env
cp .env.example .env

# Редактирование .env файла
nano .env
```

Заполните API ключи:
```env
WEATHER_API_KEY=your_openweathermap_api_key
NEWS_API_KEY=your_newsapi_key
```

Отредактируйте `config/config.yaml` под свои нужды:
```bash
nano config/config.yaml
```

### 7. Настройка аудио устройств

Проверьте доступные аудио устройства:
```bash
python3 -c "import sounddevice as sd; print(sd.query_devices())"
```

Обновите индексы устройств в `config/config.yaml`:
```yaml
audio:
  mic_device_index: 1    # Ваш микрофон
  speaker_device_index: 0  # Ваши динамики
```

### 8. (Опционально) Настройка Google Calendar

1. Создайте проект в [Google Cloud Console](https://console.cloud.google.com/)
2. Включите Google Calendar API
3. Создайте OAuth 2.0 credentials
4. Скачайте `credentials.json` и поместите в `config/google_credentials.json`

### 9. (Опционально) Настройка GPIO

Если используете кнопку активации и LED индикатор:

```
Кнопка: GPIO17 (физический pin 11) -> GND
LED:    GPIO27 (физический pin 13) -> Резистор 220Ω -> GND
```

Схема подключения:
```
[GPIO17] ----[Кнопка]---- [GND]
[GPIO27] ----[LED]----[220Ω]---- [GND]
```

## Запуск

### Ручной запуск

```bash
cd ~/rpi-voice-assistant
source venv/bin/activate
python3 src/assistant.py
```

### Автозапуск через systemd

```bash
# Копирование service файла
sudo cp voice-assistant.service /etc/systemd/system/

# ВАЖНО: Отредактируйте пути в service файле под вашего пользователя
sudo nano /etc/systemd/system/voice-assistant.service

# Замените /home/pi/ на ваш домашний каталог
# Замените User=pi на вашего пользователя

# Включение и запуск сервиса
sudo systemctl daemon-reload
sudo systemctl enable voice-assistant
sudo systemctl start voice-assistant

# Проверка статуса
sudo systemctl status voice-assistant

# Просмотр логов
sudo journalctl -u voice-assistant -f
```

## Использование

### Активация

Ассистент можно активировать:
- **Кнопкой GPIO** (если настроена)
- **Ключевым словом** ("привет ассистент", "слушай")

### Команды

#### Общение
```
"Привет, как дела?"
"Расскажи анекдот"
"Что ты умеешь?"
```

#### Погода
```
"Какая сегодня погода?"
"Сколько градусов на улице?"
```

#### Новости
```
"Расскажи новости"
"Что нового?"
```

#### Умный дом
```
"Включи свет в комнате"
"Выключи вентилятор"
"Статус света"
```

#### Напоминания
```
"Напомни через 10 минут купить молоко"
"Напомни завтра в 9:00 позвонить врачу"
"Напомни через час проверить духовку"
```

#### Google Calendar
```
"Создай встречу завтра в 14:00 с командой"
"Добавь событие на понедельник"
```

#### Системные
```
"Забудь контекст" - сброс истории разговора
"Выключись" - завершение работы ассистента
```

## Структура проекта

```
rpi-voice-assistant/
├── config/
│   ├── config.yaml           # Основная конфигурация
│   └── google_credentials.json  # Google OAuth credentials
├── models/
│   ├── vosk-model-small-ru-0.22/  # Vosk STT модель
│   └── piper/                     # Piper TTS модель
├── src/
│   ├── assistant.py          # Главный файл приложения
│   ├── modules/
│   │   ├── stt.py           # Распознавание речи
│   │   ├── tts.py           # Синтез речи
│   │   ├── llm.py           # LLM интеграция
│   │   └── activation.py    # Система активации
│   └── services/
│       ├── smart_home.py    # Умный дом
│       ├── information.py   # Погода и новости
│       └── reminders.py     # Напоминания и календарь
├── logs/                    # Логи приложения
├── requirements.txt         # Python зависимости
├── .env.example            # Пример переменных окружения
└── voice-assistant.service # systemd service
```

## Настройка и оптимизация

### Выбор LLM модели

Для Raspberry Pi 4 (4GB RAM):
```yaml
llm:
  model_name: "ai-forever/rugpt3large_based_on_gpt2"
  load_in_4bit: false
  max_new_tokens: 128
```

Для Raspberry Pi 4 (8GB RAM):
```yaml
llm:
  model_name: "IlyaGusev/saiga_mistral_7b"
  load_in_4bit: true
  max_new_tokens: 256
```

### Производительность

- **CPU температура**: Мониторинг через `vcgencmd measure_temp`
- **Охлаждение**: Рекомендуется активное охлаждение для стабильной работы
- **Swap**: Увеличьте swap до 2GB для больших моделей:
  ```bash
  sudo dphys-swapfile swapoff
  sudo nano /etc/dphys-swapfile  # CONF_SWAPSIZE=2048
  sudo dphys-swapfile setup
  sudo dphys-swapfile swapon
  ```

## Решение проблем

### Проблемы с аудио

```bash
# Проверка записи
arecord -l

# Проверка воспроизведения
aplay -l

# Тест микрофона
arecord -d 5 test.wav && aplay test.wav
```

### Проблемы с GPIO

```bash
# Проверка GPIO
gpio readall

# Добавление пользователя в группу gpio
sudo usermod -a -G gpio $USER
```

### Проблемы с памятью

```bash
# Мониторинг памяти
free -h

# Очистка кэша pip
pip cache purge

# Использование более легкой модели
```

### Логи

```bash
# Логи приложения
tail -f logs/assistant.log

# Логи systemd
sudo journalctl -u voice-assistant -n 100 -f
```

## API ключи

### OpenWeatherMap (погода)
1. Зарегистрируйтесь на https://openweathermap.org/
2. Получите бесплатный API ключ
3. Добавьте в `.env`: `WEATHER_API_KEY=your_key`

### NewsAPI (новости)
1. Зарегистрируйтесь на https://newsapi.org/
2. Получите бесплатный API ключ (100 запросов/день)
3. Добавьте в `.env`: `NEWS_API_KEY=your_key`

## Разработка и расширение

### Добавление новых команд

Редактируйте `src/assistant.py`, метод `_process_command()`:

```python
def _process_command(self, text):
    text_lower = text.lower()

    # Ваша новая команда
    if 'моя команда' in text_lower:
        # Ваша логика
        self.tts.speak("Выполняю команду")
        return
```

### Добавление новых устройств умного дома

Редактируйте `config/config.yaml`:

```yaml
smart_home:
  gpio_devices:
    - name: "моё устройство"
      pin: 24
      type: "generic"
```

## Лицензия

MIT License

## Авторы

Создано с помощью Claude Code

## Поддержка

Для вопросов и предложений создавайте issues в репозитории проекта.
